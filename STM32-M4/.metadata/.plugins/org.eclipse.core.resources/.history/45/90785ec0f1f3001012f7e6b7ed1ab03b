#include "tim.h"

void TIM2_Init(void) {
    TIM2_EN();

    // 8kHz Trigger (16MHz / 2000)
    TIM2->PSC = 0;
    TIM2->ARR = 1999;

    // Update Event (MMS=010) triggers ADC
    TIM2->CR2 |= (2 << 4);
    TIM2->CR1 |= 1; // Start Timer
}
